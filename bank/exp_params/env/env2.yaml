# number of trajectories that the agent will be allowed to perform during training
num_students: 50

# length of a trajectory
max_steps_per_episode: "standard"
#max_steps_per_episode: 15

eval_ids: "all"

reward_mode: "instant"
progression_bonus: 0
time_penalty: 0.0